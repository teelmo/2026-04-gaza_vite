import { SvgProps } from '../../types.js';
export declare const IllustrativeSuccessOnDark: ({ title, titleId, width, height, colors, ...props }: SvgProps & {
    colors?: {
        secondary: string;
        primary: string;
    };
}) => JSX.Element;
