import { SvgProps } from '../../types.js';
export declare const IllustrativeInfoOnDark: ({ title, titleId, width, height, colors, ...props }: SvgProps & {
    colors: {
        secondary: string;
        primary: string;
    };
}) => JSX.Element;
