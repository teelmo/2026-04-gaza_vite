import { SvgProps } from '../../types.js';
export declare const IllustrativeMessageOnDark: ({ title, titleId, width, height, colors, ...props }: SvgProps & {
    colors?: {
        primary: string;
        secondary: string;
    };
}) => JSX.Element;
