import { SvgProps } from '../../types.js';
export declare const LogoLastenAreena: ({ title, titleId, width, height, theme, colors, ...props }: SvgProps & {
    theme?: "default" | "light" | "dark";
    colors?: {
        LOGO_BACKGROUND: string;
        [key: string]: string;
    };
}) => JSX.Element;
