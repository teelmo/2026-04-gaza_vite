import { SvgProps } from '../../types.js';
export declare const IllustrativeMessage: ({ title, titleId, width, height, colors, ...props }: SvgProps & {
    colors?: {
        primary: string;
        secondary: string;
    };
}) => JSX.Element;
