import { SvgProps } from '../../types.js';
export declare const IllustrativeError1OnDark: ({ title, titleId, width, height, colors, ...props }: SvgProps & {
    colors?: {
        secondary: string;
        primary: string;
    };
}) => JSX.Element;
