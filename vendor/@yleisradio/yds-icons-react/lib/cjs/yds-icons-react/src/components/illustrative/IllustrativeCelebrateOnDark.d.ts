import { SvgProps } from '../../types.js';
export declare const IllustrativeCelebrateOnDark: ({ title, titleId, width, height, colors, ...props }: SvgProps & {
    colors?: {
        secondary: string;
        primary: string;
    };
}) => JSX.Element;
