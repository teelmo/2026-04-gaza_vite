import type { Theme } from '@yleisradio/yds-core';
type illustrativeColor = {
    primary: string;
    secondary: string;
};
export declare const themeColors: (theme: string) => illustrativeColor;
export declare const logoThemeColors: (theme: string) => Theme;
export {};
